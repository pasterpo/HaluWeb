# HaluWeb

HaluWeb is a single-page web app that generates absurd, fully styled fake business websites in real time using an LLM via the OpenRouter chat completions API. It shows a themed loading screen, requests complete HTML output from the model, and injects the generated page directly into the browser.
